SECRET_KEY=change-this-to-a-long-random-secret-key
DEBUG=False
ALLOWED_HOSTS=your-server-ip-or-domain
CSRF_TRUSTED_ORIGINS=http://your-server-ip-or-domain
SECURE_SSL_REDIRECT=False

DB_NAME=dbname
DB_USER=dbuser
DB_PASSWORD=dbpassword
DB_HOST=db-host
DB_PORT=5432

# Optional, defaults to the console backend (prints emails to stdout)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=
