services:
  web:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/app/data
      - static_data:/app/staticfiles

volumes:
  sqlite_data:
  static_data:
